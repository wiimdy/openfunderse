# --------------------------------------------------
# Relayer auth
# --------------------------------------------------
AUTH_SECRET=claw-local-auth-secret
ADMIN_LOGIN_ID=admin
# Prefer hash in shared environments.
ADMIN_LOGIN_PASSWORD=change_me
# ADMIN_LOGIN_PASSWORD_HASH=$2a$10$...
# Optional explicit admin allowlist. Falls back to ADMIN_LOGIN_ID.
ADMIN_IDS=admin
# Legacy fallback key (optional)
# ADMIN_EMAILS=admin

# --------------------------------------------------
# Relayer server / database
# --------------------------------------------------
# Do not set NODE_ENV in Vercel project env vars.
# `next build` must run with NODE_ENV=production.
NODE_ENV=development
RELAYER_PORT=3100
# Supabase Data API (required)
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Optional: credentials DB for NextAuth Drizzle adapter.
DATABASE_URL=

# --------------------------------------------------
# Chain runtime (required for read/write APIs)
# --------------------------------------------------
RPC_URL=https://testnet-rpc.monad.xyz
CHAIN_ID=10143
INTENT_BOOK_ADDRESS=0x76AB8571cED9F4A81b733969216F53C1e13BC835
CLAW_VAULT_ADDRESS=0xB3B6861cEE3Ee4f2018881395BBF4426C61ddc02
CLAW_FUND_FACTORY_ADDRESS=0x495CF01d2Cbab818598c707Aca8C2dCEc8001045

# --------------------------------------------------
# Aggregation / thresholds
# --------------------------------------------------
# CLAIM_THRESHOLD_WEIGHT takes precedence.
# VERIFIER_THRESHOLD_WEIGHT is a legacy alias still accepted.
CLAIM_THRESHOLD_WEIGHT=1
VERIFIER_THRESHOLD_WEIGHT=1
INTENT_THRESHOLD_WEIGHT=1
# Format: "0xValidatorA:1,0xValidatorB:2"
VERIFIER_WEIGHT_SNAPSHOT=0xf40C22fce49835867AC6599d03e9c391a1CFF912:1
# Optional allowlist override (comma-separated validator addresses).
VERIFIER_ALLOWLIST=
MAX_SUBMIT_RETRIES=3

# --------------------------------------------------
# Optional signers (only for specific flows)
# --------------------------------------------------
# For legacy bootstrap route and `npm run -w @claw/relayer factory:create-fund`.
FACTORY_SIGNER_PRIVATE_KEY=
# Optional: signer used to publish epoch snapshot roots to SnapshotBook onchain.
# Used by `POST /api/v1/funds/{fundId}/epochs/{epochId}/aggregate`.
# Fallback order: SNAPSHOT_PUBLISHER_PRIVATE_KEY -> RELAYER_SIGNER_PRIVATE_KEY -> EXECUTOR_PRIVATE_KEY
SNAPSHOT_PUBLISHER_PRIVATE_KEY=
# Optional fallback signer for factory script and legacy onchain attestation helper.
RELAYER_SIGNER_PRIVATE_KEY=

# --------------------------------------------------
# Optional execution cron (strategy bot usually executes onchain)
# --------------------------------------------------
CLAW_CORE_ADDRESS=0xd852812260138815e9aA3F3B896Ac31b6879DD92
EXECUTOR_PRIVATE_KEY=
EXECUTION_BATCH_LIMIT=5
EXECUTION_MAX_ATTEMPTS=5
CRON_SECRET=

# --------------------------------------------------
# Optional local integration endpoints
# --------------------------------------------------
RELAYER_URL=http://127.0.0.1:3100
INDEXER_URL=http://127.0.0.1:3200
EVIDENCE_STORE_URL=http://127.0.0.1:9000

# --------------------------------------------------
# Script/demo-only variables (not required by relayer runtime)
# --------------------------------------------------
ADAPTER_ADDRESS=0x0A1bd66F76dE8A73779c83c9a4b10041Ab660e9e
PARTICIPANT_PRIVATE_KEY=

# Optional: viem struct payload example (`npm run -w @claw/relayer factory:create-fund`)
# FUND_OWNER=
# STRATEGY_AGENT=0x0000000000000000000000000000000000000000
# ASSET=
# VAULT_NAME=Fund Vault Share
# VAULT_SYMBOL=FVS
# NADFUN_LENS=0x0000000000000000000000000000000000000000
# INITIAL_VERIFIERS=
# INITIAL_VERIFIER_WEIGHTS=
# INITIAL_ALLOWED_TOKENS=
# INITIAL_ALLOWED_ADAPTERS=
# CLAIM_BOOK_ADDRESS=

# Auto-sync with relayer after factory:create-fund (all optional — skipped if missing)
# FUND_ID=fund-monad-001
# FUND_NAME=Monad Meme Momentum
# STRATEGY_BOT_ID=bot-strategy-1
# STRATEGY_PRIVATE_KEY=0x...
