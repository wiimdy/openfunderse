# Participant daemon instance A (Momentum)
FUND_ID=fund-001
ROOM_ID=participant-room

# Relayer auth
RELAYER_URL=https://<your-relayer>
BOT_ID=bot-participant-a
PARTICIPANT_PRIVATE_KEY=0x...
PARTICIPANT_ADDRESS=0x...

# Monad / NadFun testnet
CHAIN_ID=10143
RPC_URL=https://monad-testnet.drpc.org

# Daemon config
PARTICIPANT_STRATEGY=A
EPOCH_SOURCE=relayer
INTERVAL_SEC=60
HORIZON_SEC=3600

# Submit safety (must enable to actually POST /claims)
PARTICIPANT_AUTO_SUBMIT=true
PARTICIPANT_REQUIRE_EXPLICIT_SUBMIT=false

# Strategy A params
PARTICIPANT_A_LOOKBACK_BLOCKS=10000
PARTICIPANT_A_CHUNK_SIZE=500
PARTICIPANT_A_WHALE_THRESHOLD_MON=1
